services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: grocerylist
      POSTGRES_USER: grocerylist
      POSTGRES_PASSWORD: DevPassword123
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
